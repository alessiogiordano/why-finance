#
#  manifest.yaml
#  Progetto di Distributed Systems and Big Data
#  Anno Accademico 2024-25
#  (C) 2024 Luca Montera, Alessio Giordano
#
#  Created by Alessio Giordano on 10/01/25.
#

kind: Namespace
apiVersion: v1
metadata:
    name: why-finance
    labels:
        name: why-finance
---
kind: Service
apiVersion: v1
metadata:
    name: stocks-database
    labels:
        app: stocks-database
    namespace: why-finance
spec:
    ports:
    - name: mysql
      port: 3306
    selector:
        app: stocks-database
---
kind: Deployment
apiVersion: apps/v1
metadata:
    name: stocks-database
    labels:
        app: stocks-database
    namespace: why-finance
spec:
    replicas: 1
    selector:
        matchLabels:
            app: stocks-database
    template:
        metadata:
            labels:
                app: stocks-database
        spec:
            volumes:
            - name: stocks-mysql-data
              emptyDir: {}
            containers:
            - name: stocks-database
              image: mysql:8.0
              imagePullPolicy: IfNotPresent # Always # Never
              ports:
              - containerPort: 3306
              volumeMounts:
              - name: stocks-mysql-data
                mountPath: /var/lib/mysql
              env:
              - name: MYSQL_ROOT_PASSWORD
                valueFrom:
                    configMapKeyRef:
                        name: why-finance-env
                        key: STOCKS_DB_PASSWORD
              - name: MYSQL_DATABASE
                valueFrom:
                    configMapKeyRef:
                        name: why-finance-env
                        key: STOCKS_DB_NAME
              startupProbe:
                  exec:
                      command:
                      - /bin/sh
                      - -c
                      - mysqladmin ping -h localhost -u root -p$MYSQL_ROOT_PASSWORD
                  failureThreshold: 20
                  periodSeconds: 10